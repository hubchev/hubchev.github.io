bookdown::gitbook:
  split_by: section
  config:
    toolbar:
      position: static
    fontsettings:
      theme: white
      family: sans
      size: 2
bookdown::pdf_book:
  keep_tex: yes
  papersize: a4
  classoption: oneside
